[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "openquant"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
manifest-path = "crates/pybridge/Cargo.toml"
